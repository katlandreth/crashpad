$('.registration_form').html("<%= escape_javascript(render partial: 'profiles/new') %>");
$('body').on('submit', '#new_profile', function(){
  console.log('form submitted');
  $('button.close').click();
})

$('#rails-flash-message').html("<%= escape_javascript(render partial: 'shared/flash_messages', locals: {flash: flash}) %>");

$(function () {
  $('[data-toggle="popover"]').popover()
})
