$('.content-container').html("<%= escape_javascript(render partial: 'members/show') %>");
$('.registration_form').html("<%= escape_javascript(render partial: 'profiles/new') %>");
$('#new_profile').on('submit', function(){
  $('button.close').click();
})

$(function () {
  $('[data-toggle="popover"]').popover()
})
